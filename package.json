{
  "name": "next-saas-rbac",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "predev": "docker-compose up -d"
  },
  "devDependencies": {
    "turbo": "2.0.3"
  },
  "workspaces": [
    "apps/*",
    "packages/*",
    "config/*"
  ],
  "packageManager": ">=bun@1.1.13",
  "dependencies": {
    "next-themes": "^0.3.0"
  }
}
